---
applications:
  - name: ai_core_api
    memory: 2048M
    buildpacks:
      - nodejs_buildpack
    command: node server.js
    services:
      - aicore-app-auth
    env:
      VECTOR_STORE_TYPE: sqlite
      NODE_ENV: production
